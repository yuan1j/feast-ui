// Feast UI runtime configuration
// Changes take effect on page refresh; no need to re-run yarn build.
// Note: this file is copied verbatim to the build directory (build/config.js).
// Keep public/config.js and build/config.js in sync; otherwise the next build
// will overwrite any modifications made in build/.
window.FEAST_UI_CONFIG = {
  // URL embedded in the Model Monitoring page
  modelMonitoringUrl: "https://www.baidu.com",
};
