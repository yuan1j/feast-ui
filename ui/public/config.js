// Feast UI 运行时配置
// 修改后无需重新执行 yarn build，刷新页面即可生效。
// 注意：该文件会原样复制到 build 目录（build/config.js），
// 请保持 public/config.js 与 build/config.js 内容一致，否则下次构建会覆盖 build 中的修改。
window.FEAST_UI_CONFIG = {
  // Model Monitoring 页面嵌入的 URL
  modelMonitoringUrl: "https://www.baidu.com",
};
